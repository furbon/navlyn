{
  "servers": {
    "navlyn": {
      "type": "stdio",
      "command": "navlyn-mcp",
      "args": [
        "--workspace",
        "${workspaceFolder}/YourRepo.sln",
        "--tool-profile",
        "reader"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
